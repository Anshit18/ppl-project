/**
 * Utility functions for the PPL project
 */

// Generates a random number
function randomSample(distribution) {
    return Math.random(); // Placeholder function
}

module.exports = { randomSample };
